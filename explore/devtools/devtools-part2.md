# DevTools Part 2

## Fixing The Bug
### Q1 Response:
The bug is that when the functions takes in the two variables as parameters, they are taken in a strings, which then cause the operation to be a string concatenation rather than a math operation.

### Q2 Response:
I would fix this by manually converting the two string values inputted into numbers, which then can allow the math operation to function ccorrectly.